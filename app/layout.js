export const metadata = {
  title: "Zalo Bot Webhook",
  description: "Zalo Bot Webhook running on Vercel",
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  );
}
